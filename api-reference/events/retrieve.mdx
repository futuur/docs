---
title: 'Retrieve event'
description: 'Get detailed information about a specific prediction market event.'
---

Retrieve detailed information about a single event by its ID, including all associated markets and their current prices.

## Endpoint

```
GET https://api.futuur.com/v2.0/events/{id}/
```

## Authentication

<Note>
  This endpoint supports HMAC authentication. Include the `Key`, `Timestamp`, and `HMAC` headers to access user-specific data. See the [authentication guide](/authentication) for signing instructions.
</Note>

## Request

<ParamField path="id" type="integer" required>
  A unique integer value identifying this event.
</ParamField>

## Response

Returns the full event object with all markets and metadata.

<ResponseField name="id" type="integer" required>
  Unique identifier for the event.
</ResponseField>

<ResponseField name="title" type="string" required>
  The prediction question.
</ResponseField>

<ResponseField name="slug" type="string" required>
  URL-friendly identifier.
</ResponseField>

<ResponseField name="status" type="string" required>
  Current state: `open`, `stopped`, `resolved`, `cancelled`, `paused`, or `reversed`.
</ResponseField>

<ResponseField name="markets" type="object[]" required>
  Array of market (outcome) objects for this event.
</ResponseField>

<ResponseField name="markets_correlation" type="string" required>
  How markets relate to each other: `linked_prices` or `independent`.
</ResponseField>

<ResponseField name="description" type="string">
  Full question details and resolution criteria.
</ResponseField>

<ResponseField name="bet_end_date" type="string">
  ISO 8601 datetime when betting closes.
</ResponseField>

<ResponseField name="resolve_date" type="string">
  ISO 8601 datetime when the event was resolved.
</ResponseField>

## Example

<CodeGroup>

```bash cURL
curl "https://api.futuur.com/v2.0/events/12345/" \
  -H "Key: YOUR_PUBLIC_KEY" \
  -H "Timestamp: 1712500000" \
  -H "HMAC: YOUR_HMAC_SIGNATURE"
```

```python Python
import requests

response = requests.get(
    "https://api.futuur.com/v2.0/events/12345/",
    headers={
        "Key": "YOUR_PUBLIC_KEY",
        "Timestamp": "1712500000",
        "HMAC": "YOUR_HMAC_SIGNATURE",
    },
)

event = response.json()
print(f"Event: {event['title']}")
for market in event["markets"]:
    print(f"  Market {market['id']}: {market['title']}")
```

</CodeGroup>

```json Sample response (200 OK)
{
  "id": 12345,
  "title": "Who will win the 2024 US Presidential Election?",
  "slug": "who-will-win-2024-us-presidential-election",
  "status": "open",
  "markets_correlation": "linked_prices",
  "description": "This market resolves based on...",
  "bet_end_date": "2024-11-05T00:00:00Z",
  "resolve_date": null,
  "markets": [
    {
      "id": 67890,
      "title": "Candidate A",
      "price": 0.55
    },
    {
      "id": 67891,
      "title": "Candidate B",
      "price": 0.42
    }
  ]
}
```
